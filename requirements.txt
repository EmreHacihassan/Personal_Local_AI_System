# ╔═══════════════════════════════════════════════════════════════════════════════════╗
# ║  ⚠️  HATIRLATMA: Bu projede ZATEN bir venv var! Yenisini oluşturmana gerek yok!  ║
# ║  📁  Konum: .\.venv\Scripts\pip.exe                                              ║
# ║  💡  Kurulum: .\.venv\Scripts\pip.exe install -r requirements.txt                ║
# ╚═══════════════════════════════════════════════════════════════════════════════════╝
#
# Enterprise AI Assistant - Requirements v3.0
# Endüstri Standartlarında Kurumsal AI Çözümü
# Güncellenme: 2026-01-21 - Gerçek çalışan sürümlerle senkronize
#
# ⚠️ PYTHON VERSION: 3.11.9 REQUIRED (DEĞİŞTİRME!)
# ⚠️ NumPy 2.0+ breaks ChromaDB - use numpy<2.0

# ============ CORE ============
python-dotenv>=1.0.0
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0

# ============ LLM ============
ollama>=0.4.0  # Updated: 0.6.1 installed
langchain>=1.0.0  # Updated: 1.2.6 installed (major version jump)
langchain-community>=0.4.0  # Updated: 0.4.1 installed
langchain-core>=1.0.0  # Updated: 1.2.7 installed
# openai>=1.12.0  # For cloud fallback - DISABLED for privacy

# ============ RAG & VECTORS ============
# ChromaDB 1.x works fine with this project despite docs saying 0.4.x
chromadb>=1.0.0  # Updated: 1.4.1 installed and working
sentence-transformers>=5.0.0  # Updated: 5.2.0 installed (major version jump)
numpy>=1.24.0,<2.0  # CRITICAL: ChromaDB incompatible with NumPy 2.0+

# ============ DOCUMENT PROCESSING ============
pypdf>=3.17.0
pdfplumber>=0.10.0
python-docx>=1.1.0
openpyxl>=3.1.2
python-pptx>=0.6.23
beautifulsoup4>=4.12.0
lxml>=5.1.0
unstructured>=0.12.0

# ============ AGENTS ============
crewai>=0.70.0  # Updated: 0.80.0 installed

# ============ API ============
fastapi>=0.115.0  # Updated: 0.128.0 installed
uvicorn[standard]>=0.30.0  # Updated: 0.40.0 installed
python-multipart>=0.0.6
websockets>=12.0
websocket-client>=1.7.0  # For frontend WebSocket client

# ============ FRONTEND ============
streamlit>=1.30.0
streamlit-chat>=0.1.1
plotly>=5.18.0  # Interactive charts for dashboard

# ============ DATABASE ============
sqlalchemy>=2.0.25

# ============ UTILITIES ============
httpx>=0.26.0
aiohttp>=3.9.0
aiofiles>=23.2.1  # Updated: 25.1.0 installed
tenacity>=8.2.3
rich>=13.7.0
psutil>=5.9.0
watchdog>=4.0.0  # Plugin hot-reload
markdown>=3.5.2  # DeepScholar HTML export

# ============ TESTING ============
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0  # Test coverage reporting
coverage>=7.4.0  # Coverage measurement

# ============ NEW: MCP PROTOCOL ============
# Model Context Protocol for Claude integration
jsonrpc-base>=2.2.0

# ============ NEW: OBSERVABILITY (Langfuse) ============
langfuse>=2.0.0

# ============ NEW: STRUCTURED OUTPUT (Instructor) ============
instructor>=0.4.0

# ============ NEW: AGENT ORCHESTRATION (LangGraph-style) ============
# Custom implementation, no extra deps needed

# ============ NEW: GRAPH RAG ============
neo4j>=5.15.0  # Optional: for production graph storage

# ============ NEW: EVALUATION (RAGAS) ============
# ragas>=0.1.0  # Optional: official RAGAS lib
# Custom implementation included

# ============ NEW: GUARDRAILS ============
# nemoguardrails>=0.7.0  # Optional: NeMo integration
# Custom implementation included

# ============ NEW: VOICE/MULTIMODAL ============
# Speech-to-Text
faster-whisper>=0.10.0  # Local Whisper STT (recommended)
# openai-whisper>=20231117  # Optional: original Whisper

# Text-to-Speech
pyttsx3>=2.90  # Local TTS (DEFAULT - fully offline)
# edge-tts>=6.1.9  # Microsoft Edge TTS - DISABLED for privacy (sends data to Microsoft)

# Vision
# llava support via Ollama, no extra deps

# ============ NEW: SCREEN CAPTURE ============
mss>=9.0.0  # Cross-platform screen capture
Pillow>=10.0.0  # Image processing

# ============ NEW: DESKTOP AUTOMATION (Computer Use Agent) ============
pyautogui>=0.9.54  # Cross-platform mouse/keyboard control
pynput>=1.7.6  # Emergency stop listener

# ============ NEW: FILE LOCKING ============
filelock>=3.13.0  # Cross-platform file locking for ChromaDB

# ============ NEW: PREMIUM FEATURES (100% LOCAL) ============
# Code Interpreter Sandbox
RestrictedPython>=7.0  # Secure Python sandbox execution

# Knowledge Graph
networkx>=3.2.0  # Graph algorithms for Knowledge Graph RAG

# Screen Recording & Analysis
opencv-python>=4.9.0  # Video processing for screen recording

# Multi-Modal RAG
PyMuPDF>=1.24.0  # PDF processing with image extraction (fitz)

# AI Security Scanner
# No extra deps needed - uses regex + LLM

# Personal Analytics Dashboard
# No extra deps needed - uses built-in modules

# ============ OPTIONAL CLOUD PROVIDERS ============
# anthropic>=0.18.0  # Claude API
# google-generativeai>=0.3.0  # Gemini API
# elevenlabs>=0.3.0  # Premium TTS
